name: autoclicker
description: A autoclicker made in flutter
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_auto_gui: ^0.0.3
  keyboard_event: ^0.3.4
  win32: ^5.0.9
  window_manager: ^0.3.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
